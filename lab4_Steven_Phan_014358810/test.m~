x = [-5, -1, 0, 2]; 
y = [-2, 6, 1, 3]; 
% 
a = newtonIter(x,y); 


% b(:1 )= y';
% for i = 2:length(x)
%     for j = i:length(x)
%         b(i,j) = (b(i-1,j-1)-b(i,j-1)) / (x(i-j+1)-x(i));
%     end
% end
% a = diag(b)';
% disp(a);
